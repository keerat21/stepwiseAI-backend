pip install fastapi uvicorn langgraph langchain langchain-google-genai nest_asyncio
pip install uvicorn
OR
python -m pip install uvicorn
python -m pip install python-dotenv

python -m uvicorn main:app --reload
